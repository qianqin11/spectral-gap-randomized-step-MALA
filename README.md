This is a repository containing simulation code and lean verification package for the paper *A global spectral gap for Metropolis-adjusted Langevin algorithm with a uniformly randomized step size*.

The folder `simulation` contains the code for reproducing the results in Section 6 of the paper.

The folder `formalization` contains the Lean package for formalizing the proofs in the paper.
